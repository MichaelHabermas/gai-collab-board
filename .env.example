# Firebase Configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123
VITE_FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# AI Configuration (multi-provider: Gemini default, or Groq)
# Provider: set VITE_AI_PROVIDER=gemini (default) or groq. Model and base URL follow from provider.
# Gemini: get key at https://aistudio.google.com/app/apikey — set GEMINI_API_KEY or VITE_GEMINI_API_KEY.
# Groq: get key at https://console.groq.com — set GROQ_API_KEY or VITE_GROQ_API_KEY.
# Dev: Vite proxy injects key. Production: set the chosen provider's key on the AI proxy service (Render) so the key stays server-side.
# VITE_AI_PROVIDER=gemini
VITE_GEMINI_API_KEY=
# VITE_GROQ_API_KEY=gsk_xxxx
# Production only: full URL of AI proxy when on a different host (e.g. Render Web Service). Include path: https://your-proxy.onrender.com/api/ai/v1
# VITE_AI_PROXY_URL=
# Production only: path on same origin (default /api/ai/v1).
# VITE_AI_PROXY_PATH=

# App Configuration
VITE_APP_NAME=CollabBoard
